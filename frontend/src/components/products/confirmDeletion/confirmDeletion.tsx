import { Dialog, Box, Typography, Button } from '@mui/material';
import type IConfirmDeletionProps from './IConfirmDeletionProps';
export default function onfirmDeletion(props: IConfirmDeletionProps) {
  return;
  <></>;
}
